import styled from 'styled-components';

const ListItem = styled.li`
  cursor: pointer;
  font-size: 14px;
  line-height: 1.5;
  padding: 10px;

  &:hover {
    background: rgba(256,256,256,0.36);
  }
`;

export default ListItem;
